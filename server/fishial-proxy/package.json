{
  "name": "catchcheck-fishial-proxy",
  "private": true,
  "devDependencies": { "wrangler": "^4.0.0", "typescript": "^5.0.0" },
  "scripts": { "deploy": "wrangler deploy", "test": "node --test tests/*.test.mjs" }
}
